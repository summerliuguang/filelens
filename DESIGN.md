# 个人文件查重与相似媒体管理系统设计

## 1. 文档目的

本文定义一个面向个人 NAS 数据的跨平台桌面程序（暂定名：`FileLens`）的产品与技术设计。它在 Windows 和 Linux 客户端运行，第一阶段主要扫描本地同步的 NAS 副本；后续可在不改变核心架构的前提下支持 SMB 映射盘和 NAS 目录。

系统不是自动清理工具。它负责发现、解释和组织候选重复项，用户逐组审核后才将选中的文件移动到客户端应用回收站。默认策略宁可漏报，也不以不可靠的相似度结果建议删除文件。

## 2. 已确认需求

| 项目 | 决策 |
| --- | --- |
| 使用者 | 单人、简体中文 |
| 客户端 | Windows 和 Linux，主要配置为 Ryzen 7 3700X、32 GB RAM、RTX 3060 Ti 8 GB |
| NAS 与数据 | ARM NAS；数据当前同步到本地磁盘。第一阶段本地优先，之后支持直接选择 SMB 映射目录 |
| 数据量 | 10 万至 100 万文件，首次索引以一夜完成为目标 |
| 比较范围 | 所有选定根目录全局互相比较；可配置排除系统、应用缓存、回收站等目录 |
| 重点类型 | 照片、视频、音频；Office/PDF 与纯文本/Markdown 文档 |
| 照片相似 | 压缩、尺寸变化、旋转、裁剪、水印等同源变体 |
| 视频相似 | 同内容转码、分辨率/封装变化；同源长短剪辑的包含关系 |
| 文档相似 | 文本内容近似；可人工确认后移入回收站，不自动建议删除 |
| 隐私 | 全部在本地处理，不上传文件、缩略图、媒体指纹或文本到云端 |
| 扫描 | 首次全量，之后手动增量；任务可暂停、恢复、取消 |
| 审核 | 并排预览、完整元数据、系统默认程序打开、每组人工确认后批量执行 |
| 保留建议 | 可配置评分，优先目录、分辨率/码率/大小、拍摄时间、原始命名等共同决定 |
| 保护 | 基于路径的保护规则，受保护文件不可被建议移入回收站 |
| 回收站 | 客户端本地集中应用回收站，默认保留 30 天；容量上限由用户配置（目标可用 100 GB 至 1 TB） |
| 数据迁移 | 项目数据库、审核决定和操作日志可导入/导出 |

## 3. 产品边界

### 3.1 第一阶段必须实现

1. 多根目录管理、排除/保护规则、可恢复全量及增量扫描。
2. 任意二进制文件的精确重复检测，按完整内容哈希确认。
3. 照片的高置信度相似检测与并排预览。
4. 视频的完整同内容转码检测，以及作为独立后台任务的同源片段包含检测。
5. 音频的精确重复和相同录音/歌曲转码检测。
6. 文档精确重复及文本近似候选；文档近似永远不产生自动删除建议。
7. 重复组审核、保留建议、受保护文件控制、移动至本地应用回收站、恢复、审计日志和到期清理。
8. Windows 10/11 x64 与主流 Linux x86_64 发行版的安装包。

### 3.2 明确不做

1. 不提供云端同步、账号体系、多人协作或远程控制台。
2. 不在第一阶段直接修改 NAS 中的文件；SMB 目录即使可扫描，默认只读，修改需在设置中单独解锁。
3. 不做跨互联网的反向搜图、版权识别、人脸识别或物体语义聚类。
4. 不承诺将任意混剪、字幕版、画中画或极短引用片段识别为同源视频。
5. 不支持 RAW 相机格式的相似识别和内置预览；RAW 文件仍可做精确哈希。该能力留作后续扩展。

## 4. 关键设计原则

1. **先廉价筛选，后昂贵确认。** 文件大小、快速哈希、媒体元数据和指纹索引用于召回；完整哈希或多帧验证用于确认。
2. **删除与检测解耦。** 检测结果永远不修改文件。移动操作只能来自用户对具体组的确认。
3. **可解释。** 每个候选结果展示匹配依据、置信度和保留评分的组成，不能只显示“AI 认为相似”。
4. **断点可恢复。** 每个阶段以持久化任务分片运行；程序退出、睡眠、盘符短暂消失后可继续。
5. **不盲信文件时间。** 创建时间在复制和同步时会变化；媒体 EXIF/容器创建时间优先，文件系统时间仅作弱信号。
6. **本地优先。** 数据库、缩略图缓存、模型、日志和回收站位于客户端。扫描网络目录时自动降低并发，避免压垮 NAS。

## 5. 用户流程

### 5.1 首次配置

1. 创建项目，选择本地同步根目录，例如 `D:\NAS-Sync\Photos`、`D:\NAS-Sync\Videos`。
2. 设置排除规则，例如 `.git`、`node_modules`、`$RECYCLE.BIN`、应用缓存目录和应用回收站目录。
3. 设置保护规则，例如 `D:\NAS-Sync\Originals\**` 永远保护。
4. 选择应用数据目录和本地回收站目录，设定 30 天期限及容量上限。
5. 选择扫描档位：默认“平衡”；当前硬件可启用 GPU 视频解码和本地向量模型加速。
6. 启动全量扫描。首页实时显示枚举、哈希、媒体分析、候选生成、确认等阶段及预计剩余时间。

### 5.2 日常增量扫描

用户点击“扫描变更”。程序快速枚举每个根目录，以路径、大小、修改时间和文件标识判断条目是否变化。未变化文件复用已有结果；新增或变化文件只重跑必要阶段。对于本地 NTFS，可选启用 USN Journal 加速变更发现；Linux 使用 inode/mtime/size 扫描。网络共享默认使用安全的目录重枚举。

### 5.3 审核和回收

1. 在“精确重复”“相似照片”“相似视频”“音频”“文档近似”队列中选择一个组。
2. 主视图并排展示候选媒体与元数据，视频可同步取样或播放，文档显示提取的文本片段差异。
3. 程序突出显示推荐保留项，显示原因、置信度和所有保护命中情况。
4. 用户逐组勾选要处理的副本；受保护项默认不可勾选，需临时解除保护规则才能处理。
5. 用户点击“执行已确认项目”。操作前再次验证源文件的路径、大小、修改时间和完整哈希，防止扫描后文件被替换。
6. 文件移动至应用回收站并记录原始路径和元数据；失败项不影响其他独立操作并显示原因。
7. 用户可恢复原路径或选择新路径。超过期限或容量超限的项目进入“待永久清理”，需再次确认后删除；不进行静默永久删除。

## 6. 检测管线

### 6.1 共同预处理

每个文件记录规范化绝对路径、根目录 ID、文件系统标识（可得时）、大小、mtime、ctime、扩展名和 MIME 推断。扫描阶段只读取元数据，不一次性加载大文件。

候选文件按下列层次处理：

1. 路径过滤：排除规则、权限错误、符号链接循环、应用数据目录和回收站。
2. 稳定性检查：读取前后比较大小与 mtime；变动中的文件延后重试。
3. 快速内容摘要：首尾各 1 MiB 加大小的 BLAKE3 摘要，用于减少完整读取。
4. 类型分析：按文件签名优先于扩展名分派给媒体/文档插件。
5. 持久化结果：每完成一个文件事务性写入 SQLite，任务可从下一个未完成分片恢复。

### 6.2 精确重复

精确重复适用于全部文件类型，结论标准是完整字节序列一致。

1. 先按文件大小分桶；唯一大小的文件不需要完整哈希。
2. 同大小桶计算快速摘要；摘要不同的文件排除。
3. 快速摘要相同的文件以流式方式计算完整 BLAKE3-256；默认分块 8 MiB，CPU 并发数为逻辑核心数减 2。
4. `size + full_hash` 相同即为精确重复组。极低概率哈希碰撞可在“严格模式”下进行逐字节比较。

预期：本地 NVMe 上，3700X 可使哈希读盘成为主要瓶颈；机械盘和 NAS 同步盘以磁盘吞吐为上限。该方案不依赖 GPU。

### 6.3 相似照片

支持 JPEG、PNG、WebP、HEIC/HEIF，优先覆盖常见手机及社交平台图片。

1. 提取 EXIF/XMP：拍摄时间、方向、像素尺寸、相机信息、GPS（仅本地保存，界面默认可隐藏）。
2. 按 EXIF 方向解码，生成最长边 512 px 的预览和缩略图。
3. 计算 dHash、pHash、颜色直方图，以及经旋转归一化的多个感知哈希。
4. 使用 pHash 汉明距离和拍摄时间窗口生成候选；图片尺寸、宽高比和颜色分布进一步筛选。
5. 对候选对使用局部特征匹配（ORB + RANSAC 单应性），验证裁剪、加水印、轻微编辑的同源关系。
6. 仅当全局指纹接近且局部内点数、内点比例、几何误差均达阈值时标为“高置信度相似”。只在感知哈希接近但无法验证局部特征时，显示为低置信度候选，不进入默认审核队列。

推荐初始阈值：同尺寸/压缩变体 `pHash <= 8`；可能裁剪候选 `pHash <= 18` 且 ORB 内点不少于 24、内点率不低于 0.35。阈值必须通过真实样本校准，并允许高级设置调整。

### 6.4 视频检测

支持 MP4、MOV、MKV、AVI 等常见容器。解析使用 FFmpeg，避免按扩展名猜测编解码器。

#### 完整同内容转码

1. 提取时长、分辨率、帧率、视频/音频编解码器、平均码率、创建时间和旋转信息。
2. 在归一化时间位置采样关键帧：10%、25%、40%、55%、70%、85%、95%，极短视频按最少 3 帧处理。
3. 对每个样本帧计算感知哈希；同时提取低频音频指纹（存在音轨时）。
4. 以时长容差 2%、多帧 hash 一致率和音频指纹相似度构建候选。
5. 只有至少 6/7 帧通过阈值，且时长、音频或连续帧序列支持同一结论时，标为高置信度完整同内容。

#### 同源片段包含

这是最昂贵的任务，必须独立启动，默认在精确重复和照片处理完成后运行。

1. 对时长超过 20 秒的视频按 2 秒间隔提取低分辨率帧指纹，构建倒排索引；为 4K 视频不保留全分辨率帧。
2. 短视频对长视频使用共享帧指纹召回，要求候选至少有 12 秒或短片时长 70% 的连续匹配窗口。
3. 对召回对进行时间偏移投票，验证匹配帧在两个视频中保持单调、近似固定偏移，排除仅共享片头、片尾或转场模板的结果。
4. 有音频时追加音频指纹对齐验证。只有视频连续对齐和（若可用）音频支持时才进入高置信度队列。
5. 结果显示“短片约从长片 `00:13:24` 开始，匹配 02:17，覆盖短片 96%”，而不是笼统的相似分数。

GPU 可用于 NVDEC 解码和 ONNX Runtime CUDA 批量帧预处理；算法结论不依赖 GPU，缺少 GPU 时降为 CPU 队列。默认限制为一个视频解码任务，用户可在夜间模式提升至两个，防止 HDD 或同步软件争抢 I/O。

第一阶段不将“视频包含”直接转化为删除建议，因为长短剪辑通常都具有保留价值。它仅用于人工审核和归档决策。

### 6.5 音频检测

支持 MP3、AAC/M4A、FLAC、WAV。精确重复仍由通用完整哈希完成。不同编码的同内容检测使用 Chromaprint/AcoustID 兼容的本地指纹算法，不调用 AcoustID 在线服务。

候选至少满足音频指纹连续匹配 30 秒或音频全长的 80%。重采样、比特率变化、标签变化通常可识别；混音、现场版和只截取数秒的音频仅显示低置信度或不显示。

### 6.6 文档检测

支持 DOCX、XLSX、PPTX、PDF、TXT、MD、CSV 和常见源代码文本。Office Open XML 通过安全解包提取文字，PDF 使用本地文本提取器；不在第一阶段对扫描 PDF 执行 OCR。

1. 规范化文本：Unicode 规范化、去除重复空白、保留段落边界；对表格和幻灯片附加结构标记。
2. 精确文本摘要识别换格式但正文相同的文档。
3. 使用 token shingles + MinHash/LSH 生成相似候选，再以余弦/Jaccard 相似度确认。
4. 仅在文本提取成功且有效文本不少于 200 字符时运行近似检测。

文档近似只显示相似段落、总体覆盖率和提取限制，绝不参与自动保留或自动删除推荐。用户可以像处理任何文件一样，人工选择移入回收站。

## 7. 保留评分与风险控制

每个重复组选择一个“建议保留项”，并不是自动执行决策。评分可见、可配置，初始权重如下：

| 信号 | 分值 | 说明 |
| --- | ---: | --- |
| 优先路径命中 | +100 | 路径保护/优先规则，例如 `Originals/**` |
| 媒体分辨率 | 0-30 | 像素总数经对数归一化 |
| 视频质量 | 0-30 | 分辨率、视频码率和帧率综合；不盲目偏向体积大的填充文件 |
| 音频质量 | 0-15 | 无损格式、采样率、码率 |
| 文件大小 | 0-10 | 仅作为同类型同内容变体的弱信号 |
| 原始命名 | +15 | 相机命名、无常见社交/下载后缀；规则可编辑 |
| 拍摄/创建时间较早 | 0-10 | 使用媒体拍摄时间优先 |
| 社交压缩迹象 | -20 | 如尺寸显著降低、常见平台文件名模式、重编码特征 |
| 受保护路径 | 不可处理 | 仍显示，但禁止被建议和默认勾选 |

对于精确重复，保留评分主要解决“保留哪个路径”；对照片和完整同源视频，质量信号更重要；对视频包含和文档近似，不生成删除建议。

执行前的强制检查：用户必须已逐组明确确认；源文件必须仍存在且与索引快照一致；目标不能命中保护规则；回收站必须有足够空间；若任一条件不满足，操作被拒绝并记录原因。

## 8. 信息架构和界面

### 8.1 页面

1. **概览**：已索引容量、可回收精确重复容量、待审核数量、上次扫描、任务状态和近期操作。
2. **来源与规则**：根目录、可用/断开状态、排除 glob、保护 glob、优先路径、扫描策略。
3. **扫描任务**：阶段进度、吞吐、当前路径、资源限额、暂停/恢复/取消、失败清单。
4. **审核队列**：精确重复、相似照片、完整同源视频、视频包含、相似音频、文档近似分别呈现；默认仅高置信度。
5. **重复组详情**：媒体并排比较、缩放、EXIF/媒体参数、路径、匹配解释、保留评分和勾选状态。
6. **回收站**：按批次、时间、原始路径筛选；恢复、导出清单、清理候选。
7. **历史与报告**：扫描和移动操作审计，导出 CSV、JSON 或自包含 HTML 报告。
8. **设置**：性能档位、GPU、阈值高级设置、缓存大小、回收站期限/容量、项目导入导出。

### 8.2 组详情关键交互

照片以并排大图为主，支持 1:1 缩放和差异叠加；视频显示关键帧时间轴和匹配区段，可调用系统默认播放器打开源文件；音频显示时长、波形摘要和指纹对齐区间；文档显示原始路径和相似文本片段。所有项目可在资源管理器/Finder 等价系统文件管理器中定位或使用默认程序打开。

批量操作只对“已人工确认”的组生效。界面不得提供“按所有推荐一键删除”。可提供“将当前组建议副本加入待执行队列”，但每个组都需要一次显式确认。

## 9. 技术架构

### 9.1 技术选型

| 层 | 推荐技术 | 原因 |
| --- | --- | --- |
| 桌面壳与 UI | Tauri 2 + React + TypeScript | Windows/Linux 安装包小、原生文件系统能力强、适合本地工具 |
| 核心服务 | Rust 2024 | 跨平台、流式 I/O、并发控制、SQLite 生态和安全的文件操作 |
| 数据库 | SQLite（WAL 模式） | 单用户本地事务、可携带、百万级索引足够；启用 FTS5 |
| 精确哈希 | BLAKE3 | 高吞吐、流式、跨平台一致 |
| 图像 | libvips + OpenCV Rust 绑定 | 高效缩略图与局部特征匹配 |
| 视频/音频解析 | FFmpeg | 容器/编解码覆盖广，支持硬件解码 |
| 音频指纹 | Chromaprint 本地库 | 成熟的转码鲁棒指纹 |
| 文本提取 | 专用 Rust/原生提取器 | Office XML、PDF、纯文本均不出本机 |
| GPU | ONNX Runtime CUDA、FFmpeg NVDEC（可选） | 3060 Ti 提升批处理和解码；CPU 回退必须可用 |
| 安装与更新 | MSI/NSIS（Windows）、AppImage/deb/rpm（Linux） | 不依赖 NAS 架构；更新包签名校验 |

Tauri 前端只通过受限命令调用 Rust 核心。路径、删除、打开外部应用等高权限操作不暴露给网页内容。所有第三方模型和 FFmpeg 构建须在发布物中列出许可证。

### 9.2 模块边界

```text
React UI
  | IPC（类型化命令、事件流）
Rust Application Core
  |- Project & Rules        项目、根目录、排除/保护/优先规则
  |- Scanner                枚举、变更判定、任务切片
  |- Fingerprint Pipeline   哈希、元数据、图像/视频/音频/文档分析
  |- Candidate Engine       分桶、LSH/倒排索引、相似度确认、分组
  |- Review Engine          保留评分、审核状态、报告
  |- File Operations        移动、恢复、预检查、冲突处理、审计
  |- Scheduler              I/O/CPU/GPU 配额、暂停恢复、失败重试
  |- Storage                SQLite、缩略图缓存、模型版本
  |- Platform Adapter       Windows/Linux 文件 ID、回收站、系统打开
  |
Local filesystem / local NAS-synced folders / optional SMB roots
```

### 9.3 并发与资源控制

调度器将任务分为 I/O、CPU 和 GPU 三类令牌。默认“夜间高性能”配置为：目录枚举 4 个任务、完整哈希 6 个任务、图片解码/特征 4 个任务、视频解码 1 个任务、GPU 推理批次最大占用 5 GB VRAM。实际值根据磁盘吞吐、温度、空闲内存动态下调。

同一物理卷通过设备标识归类，避免对单块 HDD 发起多个随机视频读取任务。检测到同步客户端正在大量写入、笔记本使用电池或可用磁盘低于安全阈值时，提示并可自动暂停。SMB 根目录使用单独、更保守的资源池。

## 10. 数据模型

SQLite 数据库位于项目目录，例如 `%LOCALAPPDATA%/FileLens/projects/<id>/index.db` 或 `~/.local/share/filelens/projects/<id>/index.db`。缩略图与大尺寸预览作为可重建缓存保存，不放入数据库。

核心表：

| 表 | 关键字段 | 用途 |
| --- | --- | --- |
| `projects` | id, name, schema_version, settings_json | 项目设置 |
| `roots` | id, project_id, path, volume_id, mode, online | 扫描根目录 |
| `rules` | id, kind, glob, enabled, priority | exclude/protect/prefer 规则 |
| `files` | id, root_id, relative_path, file_id, size, mtime, quick_hash, full_hash, state | 文件当前快照 |
| `media_metadata` | file_id, kind, taken_at, width, height, duration, codecs_json | 统一元数据 |
| `fingerprints` | file_id, algorithm, version, payload, quality | 各类指纹与版本 |
| `candidates` | id, kind, file_a, file_b, score, evidence_json, confidence | 已确认候选边 |
| `duplicate_groups` | id, kind, confidence, recommended_file_id, status | 审核呈现的连通组 |
| `group_members` | group_id, file_id, role, review_state | 组成员、保留/待处理状态 |
| `jobs` | id, type, state, cursor_json, config_json, error | 可恢复后台任务 |
| `operations` | id, type, file_id, source_path, trash_path, state, checksum, created_at | 移动、恢复、清理审计 |
| `events` | id, timestamp, severity, entity_type, entity_id, payload_json | 可导出操作日志 |

指纹携带算法和版本。更新算法不会混用旧新结果：系统将旧指纹标记为过期，以后台可恢复任务重算。

## 11. 文件操作与回收站设计

应用回收站默认位于用户选择的本地卷，如 `E:\FileLens-Recycle`。每次移动建立批次目录：

```text
E:\FileLens-Recycle\
  2026-08-31T220501Z-7f3a\
    files\<随机文件名或保留相对路径>\
    manifest.json
```

`manifest.json` 和数据库操作记录包含原绝对路径、原根目录、移动前完整哈希、大小、时间、用户操作批次、规则版本和恢复状态。若来源与回收站同一卷，优先原子重命名；跨卷则复制到临时文件、校验 BLAKE3、原子改名到回收站、最后删除源文件。任一中途失败保留源文件或临时副本，并提示用户处理，不得产生“已删除”假记录。

恢复前检查原路径：不存在则直接恢复；存在且内容哈希相同则视为已恢复；存在但不同则要求用户选择新目录，绝不覆盖。客户端回收站容量统计使用实际字节数，达到上限时按最早批次提出清理；30 天到期也只提出待清理项，需确认永久删除。

第一阶段本地根目录可执行移动。SMB 根目录默认只读审核，即使未来解锁写入，也必须要求 NAS 同共享目录指定回收站，禁止跨 SMB 移到本地回收站导致大文件复制。

## 12. 性能目标和容量估算

性能要在真实数据集验收，以下是设计目标而非绝对承诺：

| 指标 | 目标 |
| --- | --- |
| 100 万路径枚举和基础 stat | 本地 SSD 条件下 1 小时内；HDD/网络盘显著受限 |
| 仅元数据和快速摘要索引 | 本地 SSD 条件下当夜完成 |
| 全量精确哈希 | 取决于总字节数和磁盘；以连续读取速度为主，不承诺固定时长 |
| 常见照片指纹 | 10 万张在 GPU/CPU 混合处理下当夜完成，缓存缩略图可重用 |
| 视频完整转码识别 | 按总时长线性增长，可分目录/分批执行 |
| 视频包含检测 | 作为长时间专项任务；先处理目标目录，支持数天运行和断点恢复 |
| 增量扫描 | 未变化文件只 stat，变化文件局部重算 |

估算数据库 100 万文件基础记录约 0.5-1.5 GB；指纹、FTS、索引和候选会增加 2-10 GB，取决于媒体数量及视频采样密度。缩略图缓存默认上限 20 GB、LRU 淘汰且可一键重建。首次运行前应检查应用数据盘至少预留 30 GB 可用空间，视频包含任务按配置另行估算。

## 13. 安全、隐私和可靠性

1. 网络通信默认关闭。更新检查必须可关闭，且不上传项目名称、路径、哈希、指纹或遥测数据。
2. 路径、EXIF GPS、文档提取文本只保存在用户选择的本地应用数据目录。导出报告前弹窗提示其可能包含隐私路径和元数据。
3. 默认不跟随目录符号链接；若允许，使用规范路径和已访问目录 ID 防止循环。
4. 扫描使用最小权限；权限不足的路径记录为“无法访问”，不将其误判为已删除。
5. 外部解析器运行在受限子进程，限制文件大小、解码时长和内存；对损坏或恶意媒体设置超时，避免单文件阻塞队列。
6. SQLite 使用 WAL、定期 checkpoint 和启动时完整性检查。所有会修改文件的步骤写入意图日志，崩溃后可恢复或明确标注待人工处理。
7. 不永久删除原文件，除非用户在回收站清理页再次确认。操作日志不可被普通“清空历史”功能删除。

## 14. 发布阶段

### 14.1 MVP：精确重复和审核安全链路

实现项目/规则、多目录扫描、快速摘要与完整 BLAKE3、精确重复组、文件预览、路径保护、评分建议、逐组确认、本地应用回收站、恢复和审计。先以 JPEG、MP4、MP3 的元数据预览验证核心体验。

验收：可对至少 100 万条文件记录稳定恢复扫描；精确重复在完整哈希相同后零误报；中断移动操作不丢失源文件；恢复不覆盖现有不同文件。

### 14.2 V1：常见媒体相似检测

加入 HEIC/WebP、照片多级指纹与 ORB 验证、视频完整转码多帧验证、音频指纹、GPU 可选加速、审核并排比较和报告导出。

验收：基准集中的重命名、JPEG 重压缩、尺寸缩小、90/180/270 度旋转样本达到高召回；高置信度队列的误报率以人工标注集衡量并控制在 1% 以下。

### 14.3 V1.5：视频包含与文档近似

加入可暂停的帧指纹倒排索引、时间偏移验证、文本提取、MinHash/LSH 与文档片段解释。视频包含和文档近似只作为人工审核信息。

验收：在自建长短剪辑数据集上报告 precision/recall；不因共享片头、片尾或模板而默认产生高置信度包含结论。

### 14.4 V2：NAS 直接访问

支持 SMB 根目录的扫描优化、断线恢复和只读审核；在用户显式配置 NAS 同共享回收站并确认权限后才开放远程移动。ARM NAS 不运行核心索引服务，避免 NAS 资源与架构成为依赖。

## 15. 测试策略

1. 单元测试：哈希分桶、规则匹配、评分、阈值、路径规范化、回收站状态机和恢复冲突。
2. 集成测试：临时文件系统上模拟跨卷复制失败、空间不足、断电/进程终止、文件扫描中变化、权限拒绝和文件名冲突。
3. 跨平台测试：Windows NTFS、Linux ext4/btrfs；中文、空格、超长路径、大小写差异、无效 Unicode 文件名。
4. 媒体回归集：原图、重命名、重压缩、缩放、旋转、裁剪、水印、完全无关但画面相近图片；视频不同容器/码率、片段、共享片头、混剪；音频转码与不同歌曲；文档改名/轻微修改。
5. 性能测试：100 万合成路径、真实匿名媒体样本、HDD/SSD/SMB 三种介质，记录吞吐、内存、VRAM、缓存增长和恢复正确性。
6. 人工可用性测试：用户能否理解“精确重复”“同源相似”“视频包含”的区别，是否能在不误删的前提下完成一次审核与恢复。

## 16. 风险与缓解

| 风险 | 缓解 |
| --- | --- |
| 视频包含索引耗时和空间过大 | 作为可选专项任务；低分辨率稀疏采样、按目录执行、可暂停和清楚估算 |
| 相似图片误报导致误删 | 多阶段验证；默认仅高置信度；逐组人工确认；回收站可恢复 |
| 本地回收站跨卷复制缓慢/空间不足 | 操作前预估空间；可暂停；复制后哈希校验；不自动删除源；建议选择同卷回收站 |
| NAS/同步客户端与扫描争抢 I/O | 第一阶段扫描本地副本；限流和夜间模式；检测高写入时暂停 |
| 文件在扫描和操作之间被修改 | 记录快照；执行前复核大小、mtime 和完整哈希 |
| 第三方解码器处理恶意文件 | 子进程隔离、超时、资源限制、持续更新 FFmpeg/OpenCV |
| 文档提取质量不稳定 | 展示提取状态；近似结果只供人工判断；不支持 OCR 的扫描件明确标记 |

## 17. 后续待确认项

研发开始前仍需由用户确定：本地同步目录的实际路径和同步软件、回收站所在卷及准确容量上限、优先/保护目录 glob、典型文件总容量与视频总时长、是否需要 macOS、是否需要 RAW 与扫描件 OCR。这些项不阻碍 MVP 架构，但会影响默认规则、性能预估和测试样本。
