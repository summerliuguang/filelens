# FileLens 项目报告（v0.4.0）

> 报告日期：2026-09-26（审查修复批次后更新） ｜ 版本：0.4.0（四处同步：package.json / tauri.conf.json / 两个 Cargo.toml）

## 一、项目概况

FileLens 是一个**本地优先**的重复文件整理桌面应用：多目录扫描、BLAKE3 完整内容哈希确认精确重复、感知指纹（照片 dHash / 文档 SimHash）给出相似候选、可恢复的应用回收站。所有扫描、哈希、删除均在用户本机完成，不联网、不上传；「应用回收站 + 操作留痕」构成核心安全模型。

**技术栈**：Tauri 2 + React 19 + TypeScript(strict) + Rust（核心库不依赖 tauri，可独立测试与 CLI 复用）。

**当前状态**：v0.3.0 已完成原改进清单（IMPROVEMENT.md，基于 v0.2.0 的全量审查）中排定的全部 7 个批次，共 27 次提交，20 个核心库单元测试全绿，deb/rpm 安装包出包验证通过。2026-09-18 增量批次又完成第二轮审查优化的全部排定项（6 个提交）：缩略图缓存 LRU 上限、CLI busy_timeout、按批次整批恢复、全量哈希实时字节进度、保护规则预览、跨组条件批量标记、首次启动引导；26 个核心库单元测试全绿，deb/rpm/AppImage/Windows NSIS 出包验证通过，CLI 全链路冒烟（init→scan→approve→trash→restore）通过。同日 UI 批次再补三项：界面主题（跟随系统/日间/夜间）、相似照片目录筛选与整目录批量回收、相似照片大图行与并排放大对比。2026-09-23 收尾批次再落两项：照片检测新增「重复照片」视图（与「相似」并列切换）、固定式可收起侧栏。

## 二、版本演进

| 版本 | 关键内容 |
|---|---|
| **v0.1.x** | 基础能力落地：项目持久化、可取消的后台扫描、图片预览、相似照片/文档检测、图标与打包、并行哈希与缩略图缓存（性能基线） |
| **v0.2.0** | 操作历史页、回收站保留期自动清理、启动自动扫描；随后两个修复版本：相似删除接入安全链并统一确认弹窗（修复全部 8 个 P0 安全项）、审核筛选/智能标记/toast 反馈（主要 P1 体验项） |
| **v0.3.0**（本次） | 按 7 批次路线图完成：死代码清理与空态补齐 → 体验补完（busy 细化/切页保现场/扫描失败可见化/缓存治理）+ 排除规则与最小文件大小 → Schema 迁移框架 + 两级哈希 → Reveal 定位 + CSV 导出 → CI/设计令牌/前端拆分/DESIGN.md 对账 |
| **v0.3.0 增量**（2026-09-18） | 第二轮审查优化（`a9f1006` → `a94d36e`，核心库/命令层/前端三层各一笔）：按批次整批恢复、全量哈希实时字节进度、保护规则预览、跨组条件批量标记与一键回收、首次启动三步引导、缩略图缓存 500MB LRU 上限、CLI 连接补 busy_timeout；同日 UI 批次：界面主题三选（跟随系统/日间/夜间）、相似照片按目录筛选与整目录批量回收、大图行与并排放大对比 |
| **v0.3.0 收尾**（2026-09-23） | 照片检测新增「重复照片」视图（dHash 距离 0：像素一致但复制导致字节/EXIF 略异，与「相似」视图并列切换，均排除字节级精确重复）；固定式可收起侧栏（导航常驻，「«/»」收起为图标栏且状态记忆，窄窗口自动变顶部横条） |
| **功能批次**（2026-09-24） | 对照业界做法补齐四项：**① 硬链接替代删除**（处理对话框新增「转为硬链接（无损去重）」，保留所有路径、立即释放空间，安全链全走：哈希复核/跨卷与已链接检测/临时链接原子换名，CLI `hardlink`）；**② 严格模式**（设置页开关，开启后删除/回收前把副本与保留副本逐字节复核，CLI `strict-verify`）；**③ 照片 pHash 双指纹**（自实现 32×32 DCT-II pHash，schema v5 存列，候选须 dHash 与 pHash 双达阈值，UI 显示双距离，升级后首次扫描自动重建指纹）；**④ USN 快速扫描**（Windows 可选、默认关：管理员运行时从 NTFS 变更日志 MFT 序枚举文件列表替代目录递归，权限不足静默回退，schema v5 加 `files.frn`）；修复相似照片页头在 WebView2 下的挤压布局 |
| **UX 批次**（2026-09-24） | 14 项体验优化：扫描期间空态改为「扫描进行中」而非误导性空数据；相似结果 5000 对截断显式提示；概览「可清理空间」分列（精确重复 / 照片候选 / 文档候选字节数与数量）；重复目录模式细化（短目录名、结构不一致原因、只看结构一致筛选）；批量操作 toast 增「打开回收站」动作；侧栏相似照片角标；缩略图 IntersectionObserver 懒加载；大批量操作（回收/硬链接/删除）改后台任务 + 进度轮询（核心库 batch 函数加进度回调）；审核队列键盘导航（J/K/空格/Enter）；回收站「恢复到…」指定目录（核心库 restore_to）；操作历史按类型筛选；智能评分保留策略（keeper_score：原始命名/复制社交痕迹/时间早晚，query_groups 输出 suggested 徽章 + 批量策略「智能评分」）；扫描中每 5 秒静默刷新实时结果；指纹重建计数在完成消息中说明（升级预期管理）；修复 Windows release 构建缺 windows_subsystem 导致的控制台黑窗 |
| **审核排序批次**（2026-09-24） | 重复审核页排序与筛选体系重构：**默认按可释放空间排序**（size × 物理副本数 − 1，硬链接组如实记 0 不再浮顶）；组头直显「本组可释放 X」并可**折叠/展开**；组内副本按修改时间新→旧排列（配合保留策略核对）；**文件类型筛选**（图片/视频/音频/文档/压缩包）与**目录筛选**（新增 group_dirs 查询：重复文件父目录按出现次数排序，同时修 status 的 recoverable_bytes 为硬链接感知口径）；筛选栏显示已标记数量与字节数汇总；批量标记（approve_filtered）与页面筛选条件完全对齐（kind/dir 透传）；相似照片配对支持按相似程度/文件大小/修改时间排序，目录组支持按对数/可释放空间排序，整侧移除按钮显示字节数 |
| **重复页修正批次**（2026-09-24） | 用户反馈三项修正：**① 「重复照片」判定如实化**——原先 dHash=0 + pHash≤10 即宣称「像素级一致」，连拍等近似照片会混入；现改为如实描述「指纹一致候选」，点开并排对比时自动**逐像素校验**（核心库新增 `photos_pixel_identical`：解码两图、比对全部 RGBA 像素，命令 `verify_photo_pair`），弹窗顶部横幅给出「完全一致 / 像素不同 / 无法校验」结论；**② 重复目录移除归属明确化**——两个「移除」按钮从组头移入各自列头（目录名 + 张数 + 体积 + 「移除这一侧」正对它要移除的那一列），组头加「两侧互为副本，移除任一侧，副本都保留在对侧」提示；**③ 下拉框风格统一**——head-actions/filter-bar 的 select 改 `appearance:none` + 自绘箭头（明暗主题各配箭头色），底色/圆角/字重与旁边 pill 按钮一致 |
| **目录合并批次**（2026-09-25） | 重复目录模式新增「用对侧替换本侧」：想保留某个目录、但更想留对侧的文件（如 EXIF 更全的副本）时，一键完成"回收本侧重复照片 → 对侧副本移动过来"（核心库 `merge_dir_pair`：两阶段执行——先按对去重回收本侧副本，再仅当某副本引用的所有回收都成功才移动它，防止在目标目录内制造新重复；同 strcmp 安全链逐文件走保护规则与内容重哈希，移动复用 `move_indexed_file_to` 的原子换名+DB 路径更新+留痕；全部成功且对侧目录已清空时自动删除空目录）；顺带修复 `move_indexed_file_to` 的潜在唯一键冲突：目标路径存在已回收死行（present=0 占用 UNIQUE path）时把死行改名让位，keep-one 流程同样受益。同日增强：替换弹窗升级为**按前缀选择性替换**——自动识别对侧文件名前缀（字母/中文开头，含微信图片、mmexport 等中文场景），默认勾选相机/截图命名（IMG/DSC/Screenshot/PXL/Photo/WeChat/mmexport），支持自定义前缀；只有匹配前缀的对才执行替换，不匹配的保持现状（目标目录已有照片保留）；目标目录默认本侧、可改选任意目录；确认按钮实时显示替换张数、无可做操作时禁用（前端过滤 pair 列表，核心安全链不变）；ConfirmDialog 选项支持 disabled。同日再增强：原「用对侧替换本侧」「移除这一侧」合并为单个**「合并到一侧」向导**——目标文件夹三选一（左侧/右侧/其他位置：选择现有目录或输入新路径自动创建）；保留条件三条可排序优先级（前缀匹配 / 时间较新 / 优先指定目录），从上到下第一个能分出胜负的条件生效，平局时保留目标文件夹已有照片；核心 `merge_dir_pair` 泛化为 (loser, winner) 对契约：败者统一回收、胜者已在目标目录则原地保留（kept 计数），清空目录自动删除（cleanup_dirs 列表）；修复 insideDir 正反斜杠不一致导致 kept 统计恒为 0 |
| **四项增强批次**（2026-09-25，schema v6–v8） | 按评审意见落地的四项：**① 像素校验结果持久化**（v6 新表 photo_pair_verdicts 按规范化路径对 + 校验时大小/修改时间缓存判定，任一文件变化经 join 自动失效；验证任务先命中缓存、只重解码失效对，新判定批量写回）；**② 重命名/移动识别**（v7 索引 frn 与 dev+inode；扫描时把改过路径的文件按 NTFS 文件引用号 / POSIX 设备+inode + 同大小同 mtime 匹配回旧行，继承哈希与指纹只更新路径与标志，不再整文件重哈希；新路径的保护规则即时重估）；**③ EXIF 拍摄时间保留条件**（v8 files.exif_taken，扫描解析 JPEG/TIFF 的 DateTimeOriginal——kamadak-exif，无时区转换以保证副本间可比；合并向导新增第四条保留规则：有拍摄信息的优先、更早的原图优先）；**④ 实时监控扫描目录**（设置页开关，notify 后台监听 + 三秒静置防抖 + 自动增量扫描，扫描槽位 Arc 化并抽取 launch_scan 供复用）。评审修复：verdicts 90 天清理防无限增长、内容变化且解析失败时清除陈旧 EXIF、watcher 对缺失根目录跳过而非退出。Windows 39/39、Linux（WSL）40/40 测试通过，watcher 在真实应用端到端验证（创建→自动扫描→删除→自动扫描） |
| **帮助拆分与新手引导批次**（2026-09-25） | 帮助内容从视图代码拆到独立数据文件 `src/lib/help-content.ts`（纯数据 + `**加粗**`/`[[按键]]` 两种行内约定，渲染器仅十余行），后续改文案不再触碰组件；设置页 tab 状态提升到 App 层（localStorage 记忆保留），新增三处新手入口：概览页 hero「新手？查看使用帮助」、扫描来源页引导卡「查看使用帮助」、侧栏底部常驻「使用帮助」按钮，均一键跳转帮助 tab；重复审核页头部文案把「勾选的副本 = 要删除的副本」语义写明 |
| **帮助页批次**（2026-09-25） | 操作逻辑上手度评估后，项目设置页新增「项目设置 / 使用帮助」双标签（选择记忆在本地）：帮助页内置 7 个章节（三步上手、安全模型、重复审核、照片检测重复 vs 相似、相似文档、回收站与历史、扫描来源与设置）与 6 条折叠式常见问题，内容与当前实际行为同步（含逐像素校验、移除这一侧、同目录对保留一张等新语义）；复用 pill 主题切换组件与卡片样式 |
| **重复页仅显示确认重复**（2026-09-24） | 用户要求「重复的时候就只显示重复的」：**① 候选收紧**——重复照片候选从 pHash ≤10 收紧到 dHash=0 且 pHash=0 双指纹全同；**② 后台逐像素校验**——新增核心库 `verify_photo_pairs`（多线程、逐对解码比对、可协作取消、verdict 实时回调）与命令 `start_duplicate_verify`/`duplicate_verify_state`/`cancel_duplicate_verify`，候选列表变化时前端自动启动/重启校验；**③ 列表只保留确认一致的对**——重复/重复目录两视图均只显示像素校验通过的对，页头显示「逐像素校验中 done/total」进度与「已排除 N 对相似候选」汇总，空态区分无候选/校验中/校验完排除三种情况；对比弹窗保留二次校验结论横幅 |
| **收尾增强批次**（2026-09-25/26） | 六项收尾：版本号升至 0.4.0；`.gitattributes` 行尾归一；**缺席行自动清理**（90 天未回归且从未参与操作的索引行扫描后清理，完成消息报告数量）；**相似阈值可调**（设置页 4–20，立即刷新）；**扫描让核**（扫描运行时后台像素校验自动暂停、结束后续传）；**watcher 自动接管**（新增扫描目录保存后自动重启监听，缺失根目录跳过） |
| **审查修复批次**（2026-09-26，`0ef6097` → 修复批次） | 全项目四路并行审查（核心库/命令层/前端/配置文档）后的 P0/P1 修复：**① cancel_scan 状态类型错误**（Mutex vs Arc<Mutex>，取消按钮完全失效）修复；**② 已完成扫描可被新扫描覆盖**（watcher 触发的扫描无 UI 轮询，任务槽永久卡死后续扫描与后台校验——现在以内部运行状态判断）；**③ 重复视图 pHash 门与校验管线对齐**（原误用可调阈值导致候选集超出校验集、前端无限重启校验）；**④ 校验批次快照化**（原 drain 后中途暂停会丢失在途对）；**⑤ 开机自动扫描显式传参**（原闭包读首屏空状态，把已保存的排除规则覆写为空）；**⑥ 迁移补建 document_fingerprints**（该表无迁移步骤，老库升级后首个文件即扫描失败；v1 迁移测试补跑真实扫描）；**⑦ 改名检测死行让位**（目标路径被未及标记缺席的行占用时 UNIQUE 冲突并永久卡死扫描）；**⑧ 晋级失败隔离**（写线程二次哈希遇文件消失不再中止整轮扫描）；**⑨ hardlink 后刷新 Windows FRN**（原保留被删副本的文件引用号，改名后误指 keeper 行）。Windows 44/44、Linux（WSL）44/44 测试通过 |
| **P2 修复批次**（2026-09-26，`9337859` → 收尾） | 审查遗留 P2 全部落地，分五笔提交：**核心库**——跨卷移动 fallback 去掉双重复制且失败路径清理 `.filelens-partial` 临时文件；`recycle_dir_keep_one` 按 (hash, size) 分组（原仅按 hash，两个同前 64KB 的不同大文件会被误并成一组全部回收）；全部破坏性链（回收/删除/恢复/移动/硬链接）的 operations 留痕与 present 更新包进单事务（消除崩溃孤儿窗口）；USN 快扫消费式构建目录树（MFT 不再双份物化），`protect_preview`/`group_dirs` 流式聚合；补三个测试（verdict 90 天清理、watch 设置往返、EXIF 内容变化清零）。**命令层**——相似检测机制（指纹分块/配对/候选汇总/概览聚合）整体下沉核心库并 re-export（含修复：文档 SimHash 分块 8→9，鸽笼保证 ≤8/64 距离真正可检，原来距离恰为 8 的对会漏检）；12 个重命令 async 化（status/groups/相似列表/历史/回收站/导出/单对像素复核等，均 spawn_blocking，`start_duplicate_verify` 预计算挪后台且 await 期间不持槽锁）；watcher 重启全程持槽锁、任务先注册后 spawn、线程退出自查槽位（Arc 身份比对），消除竞态与「僵尸 watcher」；缩略图解码限流 3 并发（RAII permit，缓存命中不占额）。**前端**——扫描轮询容忍瞬时失败（连续 5 次才判死，防任务槽卡死）；`execute()` 失败感知（批量部分失败不再显示绿色成功 toast）；批量完成提示按「运行→结束」转折判定（相同文案的两次批量不再互相吞掉刷新）；历史页错误加载成功后清除；合并向导目标路径强制绝对路径并提示（防后端按进程 CWD 解析）、insideDir 对 Windows 盘符路径大小写不敏感（预览计数与后端一致）；合并默认前缀补「微信图片」。**配置**——dialog capability 收窄为 allow-open/allow-save。Windows 47/47、Linux（WSL）47/47 测试通过 |

v0.3.0 的 5 个提交：

| 提交 | 主题 |
|---|---|
| `8e2ff12` | 清障：删除无调用命令与 shell 插件依赖，相似文档页补「打开」，空态行动补齐 |
| `280ccd4` | 扫描失败可见化、排除规则、最小大小过滤、UI 状态保持 |
| `44b8a96` | Schema 迁移框架与两级哈希 |
| `6fff79e` | 文件管理器定位与 CSV 报告导出 |
| `bf00b98` | CI、设计令牌、视图拆分、v0.3.0 版本号、DESIGN.md 状态表 |

## 三、功能清单（对照设计文档）

### 已实现 ✅

**扫描与索引**
- 增量扫描（大小+修改时间跳过未变文件）、并行哈希管线、进度/速度/预计剩余时间、可取消
- 扫描进度横幅显示**当前正在处理的路径**；逐文件失败保留最近 20 条样本 + 总数，可展开查看
- **全量哈希实时字节进度**：横幅显示正在全量哈希的文件与本次扫描累计哈希字节，多 GB 大文件不再表现为进度停滞
- 自定义**排除规则**（路径包含匹配，Windows 忽略大小写）与**最小文件大小**（MB，扫描层强制）
- 外部删除的文件在下次扫描标记缺席；失效根目录自动跳过并在完成消息中报告

**检测**
- 精确重复：BLAKE3 + 大小，可自动处理
- **照片检测双视图**（可选切换）：
  - **重复照片**：dHash 距离 0——像素完全一致、复制导致字节/EXIF 等信息略异的对；字节级相同的精确重复自动排除（归「重复审核」）
  - **相似照片**：dHash 距离 1–4/64，仅供人工查看（带「建议保留」提示）
  - 共用：**大图行布局**、点击任一张**两张并排放大对比**、**按目录筛选**候选并一键回收整个目录的候选（走 trash_paths 安全链）
- 相似文档：TXT/MD/CSV/JSON/XML/HTML 的 SimHash ≤8/64，仅供人工查看
- **两级哈希**：>16MB 文件先比前 64KB 快速指纹，同 (大小， 快速指纹) 桶内才计算全量哈希——同大小的不同大文件不再全量读取
- 音频/视频指纹未接入，检测能力页如实标注

**审核与处理**
- 重复组筛选（路径关键字/最小大小/排序）+ 总数分页；操作后保留已加载页
- 智能标记（保留最新/最旧/最短路径）一键标记
- **跨组条件批量**：对筛选命中的**全部**分组（不止已加载页）应用一个保留策略，再一键回收全部已标记副本；受保护路径永不标记
- **硬链接识别**（schema v3）：组内同一物理文件的多个名字打「硬链接」徽章，整组互链时提示「删除副本不会释放空间」；Windows 平台如实降级为不检测
- 缩略图/放大预览（EXIF 方向、磁盘缓存 500MB LRU 自动淘汰）、系统程序打开、**文件管理器定位**
- 标记删除 → 回收站或直接删除的两步语义；>1GB 文件提示回收站双倍占用
- **保护规则预览**：设置页对未保存的规则实时统计将保护多少个已索引文件并列出示例路径
- **CSV 报告导出**（Review 工具栏 + CLI），RFC 4180 转义
- 首次启动无扫描目录时显示三步引导（添加目录 → 增量扫描 → 审核清理）

**回收站与历史**
- 移入前后哈希校验、恢复防覆盖、永久删除/清空均二次确认
- **批次视图**（schema v4）：一次批量操作的文件在回收站按批次分组展示（操作时间 + 文件数）；批量删除改为核心库单连接执行
- **按批次一键恢复**：单次调用恢复整批；原位置被占用或校验失败的文件逐条跳过并在结果中提示，批次全部恢复后重复操作为无操作
- 保留期自动过期（默认 30 天，可关；扫描结束自动清理并在消息中报告数量）
- 全量操作留痕（移入/恢复/删除），按时间倒序分页

**体验与工程**
- 全页面切换保持现场（筛选、勾选、滚动不丢）
- 按操作细化的 busy 状态（按钮只对自己所属操作禁用）
- toast 通知（成功自动消失、错误手动关闭）、Escape 关闭弹窗、focus-visible 焦点态
- 设计令牌（`:root` CSS 变量统一强调色/面板色/弱文本色/圆角），全部颜色已收敛为语义变量
- **界面主题**：跟随系统 / 日间模式 / 夜间模式，设置页切换，跟随系统实时响应操作系统深浅色变化（`data-theme` + localStorage 记忆）
- **固定可收起侧栏**：导航常驻不随内容滚动，「«/»」一键收起为图标栏（收起状态记忆），窄窗口自动变为顶部横条
- CI（核心测试 + 类型检查 + 命令层检查）、Schema 自动迁移框架（v1 起步，现为 v8）

### 未实现（Backlog 💭）

空文件夹/零字节文件视图、回收站容量上限提醒、按批次批量永久删除、相似视频/音频、完整保留评分模型。

## 四、质量与验证

**测试矩阵（44/44 绿，`cargo test --lib`，2026-09-26；Linux WSL 45/45）。审查修复批次新增三个回归测试：改名撞死行路径（UNIQUE 冲突卡死扫描）、大文件晋级遇文件消失（单文件失败隔离）、hardlink 后 Windows FRN 刷新；v1 迁移测试补跑真实扫描。

| 类别 | 覆盖点 |
|---|---|
| 安全链（8） | 扫描→审批→回收→恢复全链路；受保护文件禁止审批；相似路径删除/回收站均走保护+重哈希校验；已审批文件可直接删除；按批次整批恢复（幂等、不触碰单文件移除的记录）；**硬链接去重（路径保留、内容一致、操作留痕、重复操作行为正确）**；**硬链接拒绝内容已变化的副本且不触碰文件** |
| 严格模式（1） | **设置读写往返 + 开启后回收链路正常工作** |
| 扫描正确性（7） | 缺席标记与失效根；排除规则+最小大小；过期回收自动清理计数；错误日志环形样本；两级哈希（同前缀不同内容不误报、迟到孪生正确成组）；全量哈希字节进度上报；**USN 设置开启后经回退路径扫描结果正确** |
| 查询与数据（7） | 分组筛选/排序/总数；v1/v2 库自动迁移且数据无损；CSV 导出（含逗号路径转义）；跨组智能标记（保护跳过、keeper 选择）；保护规则预览计数与示例；**pHash 写入且指纹行被清后自动重建（确定性）** |
| 其他（5） | 照片哈希稳定性；**pHash 区分无关图片**；保护规则分隔符/大小写归一；回收站删除与清空；缩略图缓存 LRU 淘汰（最旧先删、上限内无操作、缺失目录幂等） |

**构建与冒烟**：`npm run tauri build` 产出 deb/rpm/AppImage；二进制资产嵌入验证通过；CLI 真实冒烟（init → scan → groups → approve → trash → trash-list → restore → status）行为全部符合预期，恢复后文件内容逐字完好。Windows NSIS 交叉编译包已重新产出并验证资产嵌入。CI 工作流与本地三道门一致。

**原生 Windows 构建**（2026-09-23，Windows 11 x64 + MSVC BuildTools 14.42 + Windows SDK 10.0.26100）：`cargo test --lib` 25/25 绿；`npm run tauri build -- --bundles nsis` 产出 `FileLens_0.3.0_x64-setup.exe`（约 4.5 MB），exe 内嵌前端资产（`assets/index-*.js`）验证通过。构建走标准 vcvars64 环境（rc.exe 随 SDK 提供），仅需注意 Rust 需 ≥1.85（edition 2024）。机器缺系统级 Windows SDK 时，SDK 头/库可用 xwin 下载布局后经 `INCLUDE`/`LIB` 接通、资源编译可用 llvm-rc 替代 `rc.exe`（本机验证过该替代路径后再装 SDK 回归标准方案）。

**代码规模**：前端 6 文件约 5300 行（main.tsx 798 + views.tsx 3655 + components.tsx 475 + lib 393，styles.css 1860 行另计），Rust 核心库 7032 行、命令层 1798 行。v0.2 时代 main.tsx 曾膨胀至 2091 行单文件，现已拆分为 shell / views / components / lib 四层。

## 五、架构说明

```
src/main.rs            核心库（7032 行）：扫描管线、BLAKE3/快速哈希、dHash+pHash 相似检测、
                       逐像素校验、候选配对与概览聚合、回收站、硬链接去重、目录合并、严格模式、
                       安全链、Schema 迁移、Windows USN 快扫、CLI、全部单元测试。不依赖 tauri。
src/lib.rs             核心库导出清单
src-tauri/src/main.rs  薄命令层（1798 行）：#[tauri::command]（重命令 async + spawn_blocking）、
                       serde 结构体、缩略图渲染/缓存/解码限流、后台扫描/校验/监控任务槽
src/main.tsx           App 状态与外壳（798 行）
src/ui/views.tsx       九个页面视图
src/ui/components.tsx  ConfirmDialog / PreviewModal / SimilarCompareModal / Empty / ScanProgress
src/lib/{types,format} 跨层类型镜像（snake_case 对齐）与格式化/错误翻译
```

**文件操作安全链**（所有删除类命令复用，历史上绕过校验曾是安全后门）：
查索引 → 拒绝受保护路径 → 删除前重哈希比对 → 原子移动（rename 失败走 copy+校验+删源）→ `operations` 表留痕 → 置 `present=0`。

**Schema 演进**：`ensure_initialized` 读取 `schema_version`，低于当前版本则按 `MIGRATIONS` 数组逐步事务升级（v2 新增 `files.quick_hash` 列与 `(size, quick_hash)` 索引），高于当前版本拒绝打开。老库升级无需人工干预。

**SQLite 约定**：每连接 `journal_mode=WAL` + `busy_timeout=5000`；后台扫描采用 `Mutex<Option<ScanTask>>` 任务槽 + `AtomicBool` 协作取消 + 前端 500ms 轮询。

## 六、已知限制

1. **AppImage 打包**：构建环境（WSL2）缺 libfuse2，linuxdeploy 直接运行报错、以 extract-and-run 降级模式运行会卡死在 appimagetool——AppImage 产物不可靠；deb/rpm/NSIS 三条打包路径不受影响。需要 AppImage 时建议在有 FUSE 的原生 Linux 环境出包。
2. **相似检测只读**：相似照片/文档是视觉/文本近似，误报率高于精确哈希，设计上仅允许人工逐个确认后处理，不提供自动清理。
3. **两级哈希的空扫描场景**：孤立大文件先以 `q:` 标记哈希入库（不参与分组），孪生文件出现时自动晋升为全量哈希；这意味着「先扫到孤立大文件、后续新增相同文件」的成组要等到第二次扫描。
4. **工程残留**：App 状态仍为多个 useState（未收拢为 reducer）、前端 invoke 未做集中封装、圆角/字号收敛未完成——均已记录在 IMPROVEMENT.md。

## 七、下一步建议（优先级从高到低）

1. **真实规模验证**：在 10 万+ 文件的真实目录上实测扫描耗时与两级哈希收益（DESIGN.md §12 的性能目标尚无实测数据）。
2. **桌面端人工走查**：2026-09-18 增量批次的新 UI（整批恢复、哈希进度、规则预览、跨组标记、引导）与 2026-09-23 收尾批次（重复照片视图、可收起侧栏）已经过单元测试与出包验证，尚需在真实桌面会话中做一轮点击走查。
3. **按批次批量永久删除**：批次视图与整批恢复已就绪，补上「整批永久删除」即可闭环批次清理。
4. **空文件夹/零字节检测**（P2-5）：接入现有 detector 框架。
5. **前端状态收拢**：useReducer + `lib/api.ts` 集中 invoke，根治 snake_case 对齐隐患。

---

*详细审查记录见本地 IMPROVEMENT.md（不入库）；使用说明见 [docs/MANUAL.md](MANUAL.md)。*
